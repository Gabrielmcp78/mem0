# Select the active target for zero-blip blue/green switching.
# Options: blue (existing live API on host port), green (new API in Compose).
# Default to green while verifying the new stack. Flip to blue to roll back.
# This file is mounted into /etc/nginx/conf.d and read by nginx.conf.
set $mem0_active_target green;
